1. 32gb on a separate linux box available. local machine is 16gb macbook air m2. if this is a constraint, we can use the 32gb box as the default expected constraint.
2. agree, use only afb.toml priority.
3. option A to start until we have feedback from dogfooding. will also need update and uninstall entries in afb.toml. probably also "backup" entry. at this point, afb.toml probably also needs to define some variables like a path to a common backup dir. it shall be possible for the user to add tool-specifc scripts to their config repo, and afb.toml reference these scripts (for longer scripts, eg for uninstall and cleanup). 
4. test environment means we can deploy versions of the harness without clobbering other (production) versions of the harness already on the machine. since the agent runtimes tend to auto-discover config files in common locations, it is likely that testing will pollute prod and vice versa. this is a common dev problem. we should be able to use existing tools to solve, like nix or containers. thats my thinking. im not confident a temp directory will work, because then afb has to have a way to force all the many different components to install there, and install their config there. potentially not possible (many components are immature and may not offer config surfaces to do this).
5. im open to devbox. are there other non-nix alternatives? containers? local kubernetes? opentofu?
6. i challenge your characterisation that we will need more than weeks if we use go. afb shouldnt be big. i should be able to build this with you in a weekend. sounds like Go is the right answer though. lets go with go.
8. 2 machines, daily to weekly sync? but i also dont want to have to think about it. thats more important. it could also be automated (nightly). 
9. hud was solving workflow management - user defines workflows in yaml, hud manages execution of the workflow as a statemachine with option human- gated transitions for quality management. gas city potentially solves it. ntm also potentially sovled it, but i didnt test ityet. and anyway, ntm is single maintainer, which i lean away from.
10. i was thinkng just convenience wrapper like `hatch run`. you think it should be more?
11. no more requirements. is open telemetry relevant?