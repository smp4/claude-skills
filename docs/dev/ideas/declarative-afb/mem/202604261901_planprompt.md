/new-plan create a plan to implement @docs/dev/ideas/declarative-afb/architecture.md. it shall be implemented using ATDD. decompose phases to have one acceptance test per phase. each phase must start with an acceptance test failing, and conclude with it passing. phase 0 will likely be scaffolding the project from scratch (we will implement in a completely new repo). implement best practice tools for go development, including changelog management (adhering to keep a changelog), standard commits, standard comments, ci/cd workflows for testing with github actions, precommit for tests, linting, formatting, 12 factor app principles, logging. common development commands can be packaged in a just file. make sure to write a CONCISE docs/developing.md for how to set up the dev environemtn, and perform dev tasks. 